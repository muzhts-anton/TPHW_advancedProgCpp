dist:     trusty
sudo:     required

language: c      

os:
  - linux

compiler:
  - gcc

addons:
  apt:
    packages:
      - lcov
      - valgrind
      - cppcheck
      - make
      - cmake

script:
  - ./linters/run.sh --local
  - mkdir build
  - cd build
  - cmake ..
  - make
  - ctest --output-on-failure
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then valgrind --tool=memcheck --leak-check=yes ctest; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then valgrind --tool=memcheck --leak-check=yes ./task1.out; fi
  - cd ..
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then cppcheck --enable=all --error-exitcode=1 -I include ./project/tst ./project/src ./project/include/*.h --check-config ; fi
  
after_success:
  - bash <(curl -s https://codecov.io/bash)